<Window x:Class="LittleSaveHelper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LittleSaveHelper"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Border Background="DimGray" Padding="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <ListView Name="ItemList" Grid.Column="0" Background="LightGray" MouseDoubleClick="ItemList_OnMouseDoubleClick">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Game Name" DisplayMemberBinding="{Binding GameName}" Width="105" />
                        <GridViewColumn Header="Last Backup" DisplayMemberBinding="{Binding LastBackupTime}" Width="130" />
                    </GridView>
                </ListView.View>
            </ListView>
            
            <Border Grid.Column="1" Margin="10 0 0 0">
                <StackPanel>
                    <Button Name="AddButton" Content="Add Directory" Background="LightGray" Click="AddButton_OnClick"/>
                    <Button Name="DeleteButton" Content="Delete Directory" Margin="0 10 0 0" Background="LightGray"
                            Click="DeleteButton_OnClick"/>
                    
                    <TextBlock Text="1"/>
                    <TextBox Text="BOY" IsEnabled="False"/>
                    <TextBlock Text="1"/>
                    <TextBox/>
                    <TextBlock Text="1"/>
                    <TextBox/>
                    
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
